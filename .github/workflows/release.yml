name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            gdext_lib: libgodot_mcp_gdext.so
            server_bin: godot-mcp-server
          - os: macos-latest
            target: x86_64-apple-darwin
            gdext_lib: libgodot_mcp_gdext.dylib
            server_bin: godot-mcp-server
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            gdext_lib: godot_mcp_gdext.dll
            server_bin: godot-mcp-server.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libclang-dev

      - name: Build GDExtension
        run: cargo build --release -p godot-mcp-gdext

      - name: Build Server
        run: cargo build --release -p godot-mcp-server

      - name: Rename Server binary
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            mv target/release/${{ matrix.server_bin }} target/release/godot-mcp-server-windows.exe
          elif [ "${{ runner.os }}" = "macOS" ]; then
            mv target/release/${{ matrix.server_bin }} target/release/godot-mcp-server-macos
          else
            mv target/release/${{ matrix.server_bin }} target/release/godot-mcp-server-linux
          fi
        shell: bash

      - name: Upload GDExtension library
        uses: actions/upload-artifact@v4
        with:
          name: gdext-${{ matrix.os }}
          path: target/release/${{ matrix.gdext_lib }}

      - name: Upload Server binary
        uses: actions/upload-artifact@v4
        with:
          name: server-${{ matrix.os }}
          path: target/release/godot-mcp-server-*

  package:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download GDExtension artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: gdext-*
          path: libs

      - name: Download Server artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: server-*
          path: servers

      - name: Assemble addons.zip
        run: |
          mkdir -p addons/godot_mcp/bin

          cp addons/godot_mcp/godot_mcp.gdextension addons/godot_mcp/
          cp addons/godot_mcp/plugin.cfg addons/godot_mcp/

          cp libs/gdext-windows-latest/godot_mcp_gdext.dll addons/godot_mcp/bin/
          cp libs/gdext-ubuntu-latest/libgodot_mcp_gdext.so addons/godot_mcp/bin/
          cp libs/gdext-macos-latest/libgodot_mcp_gdext.dylib addons/godot_mcp/bin/

          cd addons
          zip -r ../addons.zip godot_mcp/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            addons.zip
            servers/server-windows-latest/godot-mcp-server-windows.exe
            servers/server-ubuntu-latest/godot-mcp-server-linux
            servers/server-macos-latest/godot-mcp-server-macos
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}