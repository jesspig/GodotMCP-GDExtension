[package]
name = "godot-mcp-gdext"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "=0.5", features = ["default"] }
godot-mcp-core = { path = "../core" }
