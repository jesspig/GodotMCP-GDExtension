# v0.1.4 — Python MCP Server Phase

**Release Date**: 2026-05-26

Introduced a Python server as a separate process, collaborating with GDExtension via IPC.

### New Features

- **Python MCP server** — complete `server/src/godot_mcp_server/` implementation
  - IPC protocol data model
  - Handlers, registry, bridge
  - Editor control API
- **Server test suite** — `server/tests/` (4 test modules)
- **Tool schemas automation** — schema extraction tools under `server/tools/`
- **CI integration** — `server/pyproject.toml` + `server/setup.py`

### Refactoring

- **IPC communication refactored** — updated protocol specification and interface definitions
- **InputMap command optimization** — command implementation refactored
- **Editor control API optimization** — streamlined call chain
- **Immediate Undo/Redo** — fixed undo commit delay

### Documentation

- Full Wiki documentation updated for dual-process architecture
- Architecture documentation rewritten
