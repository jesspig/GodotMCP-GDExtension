# v0.1.3 — 健壮性增强与 E2E 测试

**发布日期**：2026-05-25

聚焦命令实现的稳定性和测试覆盖率。

### 新增

- **E2E 测试套件** —— `crates/server/tests/e2e.rs`
- **新 Wiki 模块文档**：
  - C# 解决方案生成（`csharp-solution.md`）
  - LSP 客户端（`lsp-client.md`）
  - 输入映射（`input-map.md`）
  - 插件管理（`plugin-management.md`）
  - 项目设置扩展（`project-settings-ext.md`）
  - 编辑器控制 GDExt 端（`editor-control-gdext.md`）
- **CI/CD 文档**（`ci-cd.md`）

### 重构

- **CommandHandler trait 异步化** —— `crates/server/src/handler.rs` 重构
- **注册表模式** —— WebSocket 服务器路由重写
- **WebSocket 服务器精简** —— `ws_server.rs` 代码量减少 50%（+14 / -208）
- **模块结构调整** —— 清理 `crates/server/` 内部模块

### 修复

- `play_main_scene` 获取主场景方式
- `batch_set_property` 错误提示增强
- 脚本验证与 undo 错误信息改进

### 功能改进

- `property_3d` 支持 `scale_x/y/z` 参数
- `plugin_management` 改进插件列表和启用逻辑
