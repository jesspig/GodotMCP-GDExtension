# v0.1.4 — Python MCP 服务器阶段

**发布日期**：2026-05-26

引入 Python 服务器作为独立进程，与 GDExtension 通过 IPC 协作。

### 新增

- **Python MCP 服务器** —— `server/src/godot_mcp_server/` 完整实现
  - IPC 协议数据模型
  - 处理器（handler）、注册表（registry）、桥接（bridge）
  - 编辑器控制 API
- **服务器测试套件** —— `server/tests/`（4 个测试模块）
- **tool_schemas 自动化** —— `server/tools/` 下的 schema 提取工具
- **CI 集成** —— `server/pyproject.toml` + `server/setup.py`

### 重构

- **IPC 通信重构** —— 更新协议规范和接口定义
- **InputMap 命令优化** —— 重构命令实现
- **编辑器控制 API 优化** —— 精简调用链
- **Undo/Redo 即时生效** —— 修复 undo 提交延迟问题

### 文档

- 全量 Wiki 文档更新，适配双进程架构
- 架构文档重写
