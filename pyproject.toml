[project]
name = "godot-mcp"
version = "0.2.1-dev2"
description = "Model Context Protocol bridge for the Godot 4.6+ editor (C++ GDExtension)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
]
